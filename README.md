# Advantecnia · SmartHospital

Sitio estático. `index.html` + `support.js` + `assets/`.

Despliegue en Vercel: importar el repositorio, framework "Other", sin comando de build, output directory `/`.

Formulario: envía a info@advantecnia.com vía FormSubmit. El primer envío dispara un correo de activación que hay que confirmar.
